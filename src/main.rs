mod posts;
mod gets;
mod db_reqs;
use posts::*;
use gets::*;
use db_reqs::*;

fn main() {
    //let temp_str: Vec<&str> = std::str::from_utf8(&resp).unwrap().split(" ").collect();
    //let status_code =temp_str[1];

}
